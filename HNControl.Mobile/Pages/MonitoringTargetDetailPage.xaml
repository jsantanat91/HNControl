<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="HNControl.Mobile.Pages.MonitoringTargetDetailPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Detalle monitoreo"
    Background="{StaticResource AppBackgroundBrush}">
    <ScrollView>
        <VerticalStackLayout Padding="14,12,14,24" Spacing="10">
            <Border Style="{StaticResource HeaderCard}">
                <VerticalStackLayout Spacing="4">
                    <Label x:Name="NameLabel" FontAttributes="Bold" FontSize="22" TextColor="White" />
                    <Label x:Name="ClientLabel" FontSize="13" Opacity="0.85" TextColor="#C5D5F0" />
                </VerticalStackLayout>
            </Border>

            <Border Style="{StaticResource SurfaceCard}">
                <VerticalStackLayout Spacing="4">
                    <Label x:Name="StatusLabel" FontAttributes="Bold" />
                    <Label x:Name="AddressLabel" LineBreakMode="WordWrap" />
                    <Label x:Name="ProbeLabel" FontSize="12" Opacity="0.75" LineBreakMode="WordWrap" />
                    <Label x:Name="ContractLabel" FontSize="12" Opacity="0.75" LineBreakMode="WordWrap" />
                    <Label x:Name="ServiceLabel" FontSize="12" Opacity="0.75" LineBreakMode="WordWrap" />
                    <Label x:Name="NotesLabel" FontSize="12" LineBreakMode="WordWrap" />
                </VerticalStackLayout>
            </Border>

            <Border Style="{StaticResource SurfaceCard}">
                <VerticalStackLayout Spacing="6">
                    <Label FontAttributes="Bold" Text="Ultimos checks" />
                    <CollectionView x:Name="ChecksCollection" SelectionMode="None">
                        <CollectionView.EmptyView>
                            <Label Opacity="0.7" Text="Sin historial disponible." />
                        </CollectionView.EmptyView>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
