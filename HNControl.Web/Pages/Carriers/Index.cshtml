@page
@model HNControl.Web.Pages.Carriers.IndexModel
@using HNControl.Web.Models
@{
    Layout = "_Layout";
    ViewData["Title"] = "Carriers";
}

<div class="d-flex flex-wrap align-items-center justify-content-between gap-2 mb-3">
    <div>
        <h1 class="h4 mb-1">Carriers de Internet</h1>
        <div class="text-muted">Servicios ligados a clientes + bitácora interna (tickets, incidentes, notas).</div>
    </div>

    @if (User.IsInRole(AppRoles.Admin))
    {
        <div class="d-flex gap-2">
            <a class="btn btn-outline-primary" asp-page="/Admin/Carriers/Index">Administrar carriers</a>
            <a class="btn btn-outline-secondary" asp-page="/Admin/Carriers/Services/Index">Administrar servicios</a>
        </div>
    }
</div>

<form method="get" class="d-flex gap-2 mb-3">
    <input class="form-control" name="q" value="@Model.Q" placeholder="Buscar cliente (nombre o RFC)..." />
    <button class="btn btn-primary" type="submit">Buscar</button>
    <a class="btn btn-outline-secondary" asp-page="./Index">Limpiar</a>
</form>

<div class="row row-cols-1 row-cols-lg-2 g-4">
    @foreach (var c in Model.Clients)
    {
        <div class="col d-flex">
            <div class="card hn-module accent-cyan w-100">
                <div class="card-body">
                    <div class="d-flex align-items-start justify-content-between">
                        <div>
                            <div class="hn-module-title mb-1">@c.Name</div>
                            @if (c.ServicesCount > 0)
                            {
                                <div class="text-muted small">@c.ServicesCount servicio(s) • @c.CarriersSummary</div>
                            }
                            else
                            {
                                <div class="text-muted small">Sin servicios cargados</div>
                            }
                        </div>

                        <span class="badge text-bg-light">@c.ServicesCount</span>
                    </div>

                    <div class="mt-3">
                        <a class="stretched-link hn-link" asp-page="./Client" asp-route-id="@c.ClientId">Ver detalle →</a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@if (Model.Clients.Count == 0)
{
    <div class="alert alert-light border mt-3">No hay resultados.</div>
}
